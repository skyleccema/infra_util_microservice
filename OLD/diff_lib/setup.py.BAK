from setuptools import find_packages, setup

setup(
    name="diff_lib",
    packages=find_packages(include=['diff_lib']),
    version='0.1.0',
    description="My first lib in python: Difference library",
    author="Me",
    install_requires=['pytest-runner', 'pytest==4.4.1'],
    test_suite='tests',
)